<Page
    x:Class="BlackJackDudekGueguen.View.TableListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlackJackDudekGueguen.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="tableTemplate">
            <StackPanel>
                <TextBlock Text="{Binding Table.FirstName}" />
                <TextBlock Text="{Binding Table.LastName}" />
                <TextBlock Text="{Binding Table.Age}" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListBox x:Name="listBox" ItemsSource="{Binding UpdateSourceTrigger=PropertyChanged, TableList}" ItemTemplate="{StaticResource tableTemplate}" HorizontalAlignment="Left" Width="140"></ListBox>
    </Grid>
</Page>
